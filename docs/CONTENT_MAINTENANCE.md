# Content maintenance

Edit company facts and launch wording in `assets/js/company.js`. Cinqic officially launched August 2, 2026 (`launchState: "launched"`); do not revert that. Juniper Math 1's launch is tracked separately under `juniper.launchDate` / `juniper.launchState` and is planned for August 27, 2026 — keep `juniper.launchState` at `"pre-launch"` until the owner deliberately updates it on or after that date. Update Juniper Math 1's development status only from the official `Cinqic/juniper-math-1` repository.

Keep the homepage focused on Cinqic and Juniper Math 1. Mark current work as in development, future work as planned or long-term, and do not add availability, performance, privacy, licensing, or open-source claims without evidence. Treat the Cinqic Calculator relationship as planned until the Juniper Math 1 repository confirms an implemented integration. Replace `assets/img/cinqic-social.svg` only with an approved asset and keep its absolute HTTPS metadata URL accurate.
